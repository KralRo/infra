apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: myapp-ingress
  namespace: ingress-nginx
spec:
  rules:
    - host: web2r.cz
      http:
        paths:
          -backend:
            serviceName: myapp-service
            servicePort: 5000